TODO list for Cerberus:

[Network]
-Implement sockets (UDP, TCP, HTTP/HTTPS(websockets), FTP, ICMP)
-Test sockets


[Database]
-TEST the convertion system
-Implement a custom thread that takes ownership of an sql database and processes a queue of transactions from other threads on the same db 


[Memory]
-Implement a system to link external libraries at runtime (aka plugins)

